<form method="post" action="{{ path('app_excercise_delete', {'id': excercise.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ excercise.id) }}">
    <button class="btn">Delete</button>
</form>
